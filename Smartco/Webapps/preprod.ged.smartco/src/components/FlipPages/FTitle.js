import React from 'react';

const FTitle = ({ children }) =>
    <div style={{ fontSize: 15, marginBottom: 10, textTransform: 'uppercase',color:"#000"}}>
      {children}
    </div>;

export default FTitle;
